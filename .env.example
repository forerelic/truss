# ==============================================
# Truss Monorepo - Environment Configuration
# ==============================================
# ⚠️  IMPORTANT: Package-Specific Structure
# ==============================================
# This monorepo uses PACKAGE-SPECIFIC .env files (best practice).
# DO NOT create a root .env.local file.
#
# Instead, create .env.local files in each app directory:
#   - apps/web/.env.local       (see apps/web/.env.example)
#   - apps/precision/.env.local (see apps/precision/.env.example)
#   - apps/momentum/.env.local  (see apps/momentum/.env.example)
#
# This prevents environment variable leakage between apps
# and better models runtime behavior.
#
# ==============================================
# Global Environment Variables (Optional)
# ==============================================
# These are shared across all apps and can be set here:

# Turborepo Remote Caching
TURBO_TOKEN=
TURBO_TEAM=

# Supabase CLI Access
SUPABASE_ACCESS_TOKEN=

# Project References (for CI/CD)
STAGING_PROJECT_ID=
PRODUCTION_PROJECT_ID=

# ==============================================
# CI/CD (GitHub Actions)
# ==============================================
# These should be set as GitHub Secrets, not in .env files:
# - TURBO_TOKEN
# - TURBO_TEAM
# - SUPABASE_ACCESS_TOKEN
# - PRODUCTION_PROJECT_ID
# - STAGING_PROJECT_ID
# - TAURI_SIGNING_PRIVATE_KEY
# - APPLE_CERTIFICATE
# - WINDOWS_CERTIFICATE